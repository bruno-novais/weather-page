/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['openweathermap.org', 'example.com', 'another-example.com', 'images.unsplash.com'],
    },
};


export default nextConfig;
